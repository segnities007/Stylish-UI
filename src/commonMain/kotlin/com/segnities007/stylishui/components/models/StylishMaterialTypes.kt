@file:OptIn(androidx.compose.material3.ExperimentalMaterial3Api::class)

package com.segnities007.stylishui.components.models

/** Values used by modal-sheet transition confirmation. */
public typealias StylishSheetValue = androidx.compose.material3.SheetValue

// Compatibility types for the flexible component API. These preserve the existing binary
// contract; the constrained screen vocabulary intentionally does not expose these types.
/** Stylish import for the existing ButtonColors compatibility contract. */
public typealias StylishButtonColors = androidx.compose.material3.ButtonColors

/** Stylish import for the existing ButtonElevation compatibility contract. */
public typealias StylishButtonElevation = androidx.compose.material3.ButtonElevation

/** Stylish import for the existing CardColors compatibility contract. */
public typealias StylishCardColors = androidx.compose.material3.CardColors

/** Stylish import for the existing CheckboxColors compatibility contract. */
public typealias StylishCheckboxColors = androidx.compose.material3.CheckboxColors

/** Stylish import for the existing ColorScheme compatibility contract. */
public typealias StylishColorScheme = androidx.compose.material3.ColorScheme

/** Stylish import for the existing DatePickerColors compatibility contract. */
public typealias StylishDatePickerColors = androidx.compose.material3.DatePickerColors

/** Stylish import for the existing DatePickerFormatter compatibility contract. */
public typealias StylishDatePickerFormatter = androidx.compose.material3.DatePickerFormatter

/** Stylish import for the existing DatePickerState compatibility contract. */
public typealias StylishDatePickerState = androidx.compose.material3.DatePickerState

/** Stylish import for the existing DateRangePickerState compatibility contract. */
public typealias StylishDateRangePickerState = androidx.compose.material3.DateRangePickerState

/** Stylish import for the existing DisplayMode compatibility contract. */
public typealias StylishDisplayMode = androidx.compose.material3.DisplayMode

/** Stylish import for the existing DrawerState compatibility contract. */
public typealias StylishDrawerState = androidx.compose.material3.DrawerState

/** Stylish import for the existing DrawerValue compatibility contract. */
public typealias StylishDrawerValue = androidx.compose.material3.DrawerValue

/** Stylish import for the existing ExposedDropdownMenuAnchorType compatibility contract. */
public typealias StylishExposedDropdownMenuAnchorType = androidx.compose.material3.ExposedDropdownMenuAnchorType

/** Stylish import for the existing ExposedDropdownMenuBoxScope compatibility contract. */
public typealias StylishExposedDropdownMenuBoxScope = androidx.compose.material3.ExposedDropdownMenuBoxScope

/** Stylish import for the existing IconButtonColors compatibility contract. */
public typealias StylishIconButtonColors = androidx.compose.material3.IconButtonColors

/** Stylish import for the existing IconToggleButtonColors compatibility contract. */
public typealias StylishIconToggleButtonColors = androidx.compose.material3.IconToggleButtonColors

/** Stylish import for the existing NavigationItemColors compatibility contract. */
public typealias StylishNavigationItemColors = androidx.compose.material3.NavigationItemColors

/** Stylish import for the existing NavigationItemIconPosition compatibility contract. */
public typealias StylishNavigationItemIconPosition = androidx.compose.material3.NavigationItemIconPosition

/** Stylish import for the existing NavigationRailItemColors compatibility contract. */
public typealias StylishNavigationRailItemColors = androidx.compose.material3.NavigationRailItemColors

/** Stylish import for the existing RadioButtonColors compatibility contract. */
public typealias StylishRadioButtonColors = androidx.compose.material3.RadioButtonColors

/** Stylish import for the existing SearchBarColors compatibility contract. */
public typealias StylishSearchBarColors = androidx.compose.material3.SearchBarColors

/** Stylish import for the existing SegmentedButtonColors compatibility contract. */
public typealias StylishSegmentedButtonColors = androidx.compose.material3.SegmentedButtonColors

/** Stylish import for the existing SelectableChipColors compatibility contract. */
public typealias StylishSelectableChipColors = androidx.compose.material3.SelectableChipColors

/** Stylish import for the existing SelectableDates compatibility contract. */
public typealias StylishSelectableDates = androidx.compose.material3.SelectableDates

/** Stylish import for the existing SheetState compatibility contract. */
public typealias StylishSheetState = androidx.compose.material3.SheetState

/** Stylish import for the existing BottomSheetScaffoldState compatibility contract. */
public typealias StylishBottomSheetScaffoldState = androidx.compose.material3.BottomSheetScaffoldState

/** Stylish import for the existing SliderColors compatibility contract. */
public typealias StylishSliderColors = androidx.compose.material3.SliderColors

/** Stylish import for the existing SnackbarData compatibility contract. */
public typealias StylishSnackbarData = androidx.compose.material3.SnackbarData

/** Stylish import for the existing SnackbarDuration compatibility contract. */
public typealias StylishSnackbarDuration = androidx.compose.material3.SnackbarDuration

/** Stylish import for the existing SnackbarHostState compatibility contract. */
public typealias StylishSnackbarHostState = androidx.compose.material3.SnackbarHostState

/** Stylish import for the existing SnackbarResult compatibility contract. */
public typealias StylishSnackbarResult = androidx.compose.material3.SnackbarResult

/** Stylish import for the existing SnackbarVisuals compatibility contract. */
public typealias StylishSnackbarVisuals = androidx.compose.material3.SnackbarVisuals

/** Stylish import for the existing SwitchColors compatibility contract. */
public typealias StylishSwitchColors = androidx.compose.material3.SwitchColors

/** Stylish import for the existing TextFieldColors compatibility contract. */
public typealias StylishTextFieldColors = androidx.compose.material3.TextFieldColors

/** Stylish import for the existing TextFieldLabelPosition compatibility contract. */
public typealias StylishTextFieldLabelPosition = androidx.compose.material3.TextFieldLabelPosition

/** Stylish import for the existing TextFieldLabelScope compatibility contract. */
public typealias StylishTextFieldLabelScope = androidx.compose.material3.TextFieldLabelScope

/** Stylish import for the existing TimePickerColors compatibility contract. */
public typealias StylishTimePickerColors = androidx.compose.material3.TimePickerColors

/** Stylish import for the existing TimePickerLayoutType compatibility contract. */
public typealias StylishTimePickerLayoutType = androidx.compose.material3.TimePickerLayoutType

/** Stylish import for the existing TimePickerState compatibility contract. */
public typealias StylishTimePickerState = androidx.compose.material3.TimePickerState

/** Stylish import for the existing TopAppBarColors compatibility contract. */
public typealias StylishTopAppBarColors = androidx.compose.material3.TopAppBarColors

/** Stylish import for the existing TopAppBarScrollBehavior compatibility contract. */
public typealias StylishTopAppBarScrollBehavior = androidx.compose.material3.TopAppBarScrollBehavior

/** Stylish import for the existing Typography compatibility contract. */
public typealias StylishTypography = androidx.compose.material3.Typography

/** Stylish import for the existing SwipeToDismissBoxState compatibility contract. */
public typealias StylishSwipeToDismissBoxState = androidx.compose.material3.SwipeToDismissBoxState

/** Stylish import for the existing SwipeToDismissBoxValue compatibility contract. */
public typealias StylishSwipeToDismissBoxValue = androidx.compose.material3.SwipeToDismissBoxValue

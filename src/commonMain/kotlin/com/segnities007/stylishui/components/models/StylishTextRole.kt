package com.segnities007.stylishui.components.models

/** Semantic typography roles; headings also expose accessibility heading semantics. */
public enum class StylishTextRole {
    Display, Heading, Title, Body, Supporting, Label,
}
